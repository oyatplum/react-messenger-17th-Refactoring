export interface UserInfo {
  userId: number;
  userName: string;
}

export interface MessageInfo {
  addText: string;
  messageId: number;
  userNum: number;
  date: string;
}

export interface ChatInfo {
  addText: string;
  userNum: number;
  date: string;
}
